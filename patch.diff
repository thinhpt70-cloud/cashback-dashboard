--- src/components/shared/SharedTransactionsDialog.jsx
+++ src/components/shared/SharedTransactionsDialog.jsx
@@ -413,11 +413,11 @@
                 </div>
             </div>

+            {/* Mobile Header (Filters) - Moved OUTSIDE scroll area so dropdowns aren't clipped */}
+            <div className="md:hidden px-6 pt-2 pb-1 relative z-50">
+                {renderMobileFilters()}
+            </div>
+
             <div className="flex-1 overflow-y-auto pb-0 pt-0 flex flex-col">
-                    {/* Mobile Header (Filters) - Rendered INSIDE scroll area but sticky */}
-                    <div className="md:hidden px-6">
-                    {renderMobileFilters()}
-                    </div>
-
                     {/* Mobile Bulk Bar - Rendered OUTSIDE normal flow but fixed via CSS */}
                     <div className="md:hidden px-6">
